#include "deletewindow.h"
#include "deletewindow.h"

DeleteWindow::DeleteWindow(QWidget *parent) :
    QDialog(parent),
    ui(new Ui::delete)
{
    ui->setupUi(this);
}

DeleteWindow::~DeleteWindow()
{
DeleteWindow ui;
}
